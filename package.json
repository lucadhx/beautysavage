{
  "name": "beautysavage",
  "private": true,
  "description": "BeautySavage — point d’entrée UNIQUE du développement. `npm run dev` lance backend (6100) + manager (6101) + vitrine (6102) via scripts/dev-canonical.mjs.",
  "scripts": {
    "dev": "node scripts/dev-canonical.mjs",
    "backend": "npm run dev:app --prefix backend",
    "manager": "npm run dev --prefix manager -- --port 6101 --host",
    "vitrine": "npm run dev --prefix vitrine -- --port 6102 --host",
    "test": "npm test --prefix backend && npm test --prefix manager && npm test --prefix vitrine",
    "build": "npm run build --prefix vitrine && npm run build --prefix manager"
  }
}
