# URL initiale publique du backend — À LAISSER VIDE en développement local :
# le proxy Vite (même origine) route /api et /uploads vers le backend 6070.
# Définir une valeur force des appels CROSS-ORIGIN (CORS) et casse le bootstrap.
# ngrok/mobile : mettre l'URL https du backend. Prod : https://api.domaine.com
# VITE_API_URL=
