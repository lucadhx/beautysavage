# packages/api-client

Client HTTP type (fetch credentials:include), hooks TanStack Query, mapping codes erreur -> UX. Enveloppe { ok, ... } / { ok:false, error, code } (cf. rapport 139). Base URL = same-origin (proxy /api).
