# packages/auth

Session (GET /auth/me), login (POST /auth/login, gere blocked/reason onboarding), guards RequireAuth / RequireRole. Cookie beautysavage_session (HttpOnly) — autorite = backend (401/403).
