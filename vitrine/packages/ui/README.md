# packages/ui

Design system partage. Theme dynamique depuis /api/vitrine/theme (CSS custom properties). Composants : Layout, DataTable, formulaires CRUD, calendrier, StatePage (maintenance/contrat/suspension), PriceTag. CSS Modules vs Tailwind a trancher en R0.
